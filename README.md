Created a custom Nginx Docker image serving a personalized HTML page, deployed it on an AWS EC2 instance using Docker Compose with a bind mount at /var/opt/nginx. 
Finally, tagged the custom image and pushed it to Docker Hub for remote storage and reuse.
